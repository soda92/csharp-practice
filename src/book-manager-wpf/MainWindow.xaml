<Window x:Class="book_manager_wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:book_manager_wpf"
        mc:Ignorable="d"
        Title="Book Manager" Height="450" Width="800" Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10">
            <Label Content="ISBN:"/>
            <TextBox x:Name="txtISBN" Width="100"/>
            <Label Content="Name:"/>
            <TextBox x:Name="txtName" Width="150"/>
            <Label Content="Author:"/>
            <TextBox x:Name="txtAuthor" Width="150"/>
            <Label Content="Count:"/>
            <TextBox x:Name="txtCount" Width="50"/>
        </StackPanel>

        <DataGrid x:Name="dgBooks" Grid.Row="1" Margin="10" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ISBN" Binding="{Binding ISBN}" Width="SizeToCells"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Author" Binding="{Binding Author}" Width="*"/>
                <DataGridTextColumn Header="Count" Binding="{Binding Count}" Width="SizeToCells"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="2" Margin="10">
            <Button Content="Add" Click="AddButton_Click" Width="75" Margin="5"/>
            <Button Content="Modify" Click="ModifyButton_Click" Width="75" Margin="5"/>
            <Button Content="Delete" Click="DeleteButton_Click" Width="75" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
